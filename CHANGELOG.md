# 更新日志

## 0.2.0

**导入与识别**
- 修复研究生课表无法导入（无课程代码、教师无工号）
- 修复课程名以括号数字结尾时被误判为教师工号、整行丢弃
- 修复同一格叠放多门课时整格检索不到
- 修复同一节课被重复识别成两条
- 新增防误抓校验：卡片须落在周几列与节次范围内，方括号内须是周次写法
- 新增「排课信息」浮层读取，星期与节次取自文字而非位置推算
- 浮层与卡片按格合并：浮层失败的格子回退到卡片，最坏情况不劣于从前
- 重复录入内容自动去重，同课不同周次、不同教师不受影响
- 修复叠放格节次算错：改为按「格子」而不是文字层高度确定节次，不再出现 5-5、6-6 这类错误区间，中间那段也不再丢失
- 修复浮层条目折行导致整条丢失：工号、地点被从中间断开的续行会拼回
- 修复同一格多条排课被拼到同一行时只读到第一条：改为按 [节次] 逐条切开，不再把第二条吞进上一条的地点
- 修复一格内文字层合并（格内多条排课同属一个元素）时整格读不到：改为按下一条的教师(工号)逐条切开
- 修复带「1. 2.」序号的条目只读到没有序号的那条，序号也不再残留
- 修复研究生格式（无课程代码、无教师工号）整行读不到：一行用逗号并排的两段周次与地点，现在各取自己的周次并合并为一节课
- 修复同一门课按周次分段、教师不同时被拆成多门课，现合并为一节课，教师逐条记录
- 修复同一节课地点详略不同（「四平路校区」与「彰武北大楼 409 四平路校区」）时被拆开
- 修复多段周次合并时断档被填满：周次之间有间隔时不再凭空补上没课的周次
- 修复学期改短后旧的补课周次越界导致弹窗崩溃

**课程详情**
- 新增点按课程查看详情：列出教师、地点、星期、节次、周次与具体日期
- 详情顶部按 1 系统「排课信息」同样的写法给出摘要，便于逐项对照排查识别错误
- 同一门课多个时间段全部列出，并标出所点的那一节
- 修复整周课表中远处的卡片点不动：命中顺序改为官方 InteractiveViewer 的写法（OverflowBox 在 Transform 外层），右下方卡片也能点开

**提醒与通知**
- 修复有强通知时本应用提醒不显眼
- 修复已显示的提醒被应用自己清除
- 备忘录提醒文案改为「距离事件开始」
- 新增备忘录专用提前时间设置

**桌面小组件**
- 修复倒计时出现负数
- 修复倒计时不自动刷新，只有点开 App 才更新
- 新增小组件独立配色，文字颜色自动对比
- 新增刷新频率三档：省电、正常、精确（显示到秒）

**界面与外观**
- 新增整周课表页：整学期网格、可缩放拖动、重叠课程并排
- 修复整周课表缩放回弹、默认缩放过大
- 修复整周课表节次时间被遮挡
- 新增左右滑动切换日期，可选一天或一周，并带滑入动画
- 新增主题色自选：12 个预设色加 HSL 自定义取色
- 新增主界面背景图，支持裁剪预览、遮罩浓度与课表透明度调节
- 修复背景图未铺满顶部区域、背景图下文字看不清
- 修复窄屏下日期栏溢出错位
- 整周课表卡片也可点开课程详情

**新增功能**
- 新增备忘录，支持按周重复与一次性两种时间
- 备忘录进入课表、参与提醒、显示在桌面小组件
- 新增课上完后自动显示第二天课表
- 教程扩充至 12 步，覆盖全部新功能

**调休与补课**
- 调休日设置改为直接选“要补哪一天”的日期，教学周和星期自动对应，仍可用下拉框微调
- 调休提示条写明补的是第几周周几，并新增“取消”恢复按实际周次上课
- 取消替代课表后保留调休日记录，提示条不会消失，可随时重新设置

**其它**
- 应用名统一为 TJ Class Schedule
- 教学周与日期互换逻辑收拢到 `Term`，引擎改为复用，消除重复实现
- 自动化测试从 63 项增至 216 项

## 0.1.0

- 支持同济统一认证登录和个人课表网页读取
- 支持桌面网页模式，改善手机端课表显示不全
- 只解析课表区域的真实课程卡片，并按星期和节次排序
- 支持 CSV、XLSX、图片识别和手工添加课程
- 支持教学周、单双周、不连续周次及一门课多个时间段
- 支持今日课表、本周课表、快速切换教学周和下一节课倒计时
- 支持国家法定节假日、调休补班和目标教学周课表
- 支持本地精确通知、三档声音与震动模式、锁屏提示
- 支持桌面下一节课小组件
- 支持长按修改、临时停课、删除确认和课程冲突检测
- 支持导入差异比较、合并或替换、撤销导入
- 支持本地 JSON 备份与恢复
- 支持提醒权限、精确闹钟权限和排程数量检查
- 增加国产 Android 后台设置教程和作者联系方式
