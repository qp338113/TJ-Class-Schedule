# TJ Class Schedule 更新报告

版本 **0.2.0** ｜ 2026 年 9 月 18 日

这次更新集中在三件最影响日常使用的事上：**课表认得准不准、提醒到不到、桌面小组件动不动**。此外新增了备忘录、整周课表和调休补课两大块能力。

下面按使用场景分组，你可以只挑自己关心的部分看。

---

## 一、课表识别与导入

这是本轮改动最大、也最容易被察觉的一块。此前有几类课表会导入失败或导入出错，现在都已修复。

**修复的问题**

- **研究生课表无法导入。** 这类课表没有课程代码、教师名后面也不带工号，旧版本会整行丢弃。现已支持。
- **课程名以括号数字结尾时整行丢失。** 例如课程名末尾形如 `(2)`，会被误判成教师工号，导致这一行被丢掉。现在能正确区分。
- **同一格叠放多门课时搜不到。** 一个格子里叠了好几门课的情况，旧版本会整格检索失败。
- **同一节课被重复识别成两条。** 重复录入的内容现在会自动去重；同一门课的不同周次、不同教师不会被误合并。
- **叠放格节次算错。** 曾经出现 `5-5`、`6-6` 这类互不重叠的错误节次，中间那段还会整条丢失。根因是节次按「卡片文字层自身高度」去数，现在改按**格子**确定——同一格里的课程都属于该格覆盖的节次。
- **一格内多条排课被拼到同一行时只读到第一条。** 现在按 `[节次]` 逐条切开，不再把第二条吞进上一条的「地点」。
- **一格内文字层合并（多条排课同属一个元素）时整格读不到。** 现在按下一条的「教师(工号)」逐条切开。
- **条目折行导致整条丢失。** 浮层宽度有限，长条目会折行，工号或地点会被从中间断开（如 `(0911` + `8)`）。现在会把续行拼回，工号和地点不再截断。
- **带 `1.` `2.` 序号的条目只读到没有序号的那条。** 现已支持，且序号不会残留在内容里。
- **同一门课按周次分段、每段不同老师时被拆成好几门课。** 现在合并为**一节课**，每位老师各记在自己负责的周次上，不再显示成几节互相冲突的课。
- **同一节课地点详略不同时被拆开。** 例如一段写「四平路校区」、一段写「彰武北大楼 409 四平路校区」，现在识别为同一节课，取更具体的那条。
- **多段周次合并时把断档填满。** 周次之间有间隔时，不再凭空补上没课的周次。
- **学期改短后旧的补课周次越界，设置弹窗报错。** 现在会自动夹回有效范围。

**识别精度提升**

- **新增「排课信息」浮层读取。** 旧版本靠元素位置反推星期和节次，遇到特殊排版就会算错。现在直接读取页面浮层里的文字，星期、节次、周次都以文字为准。
- **浮层与卡片按格合并。** 浮层读取失败的格子会自动回退到原来的卡片解析，所以**最坏情况不会比旧版本差**。
- **新增防误抓校验。** 课程卡片必须落在合理的周几列与节次范围内，方括号内必须是周次写法，避免把页面上的其它文字误当成课程。

**效果**：完整课表的回归校验为 37 条，漏抓 0 条、多抓 0 条。

---

## 二、点课程看详细信息（新）

在课表里点一下任意课程，会弹出这门课的详细信息，把 **App 实际保存**的内容逐项列出来。

**包含什么**

- 顶部按 1 系统「排课信息」的同样写法给出一段「信息摘要」，例如 `[周四] 排课信息` 加 `[5-6节] [1-12周] Auto CAD工程制图 徐俊 土木学院机房`。
- 逐项字段：**教师 / 地点 / 星期 / 节次 / 周次 / 上课周 / 时间段起止**，以及这节课对应的**具体日期与教学周**。
- 「上课周」列出实际会上的每一周（如 `1, 2, 3, … 12`），单双周、断档有没有解析错，一眼可见。
- 同一门课有多个时间段时**全部列出**，并高亮你点的那一节。

**主要用途是查错**：万一导入的结果和网页上不一致，把网页的「排课信息」和这里的「信息摘要」逐行对照，就能看出是星期、节次、周次、教师还是地点读错了。

**操作**：点按=看详情，**长按=修改**，两个手势分开，避免想查看时误改配置。主界面和整周课表的卡片都支持。

---

## 三、调休与补课（新）

国家调休上班日需要按学校通知补课，但国家通知只会说「哪天上班」，**不会说补哪一天的课**，所以这一步必须由你来指定。

**设置方式**

- 到了调休日，主界面会出现一条橙色提示条。点「设置」，**直接选「要补哪一天」的日期**（例如 9 月 20 日补第 4 周周二的课，就选那天对应的日期），**教学周和星期会自动对应**，不需要你自己心算。
- 如果学校通知里写的是「第几周周几」，也可以用下面的两个下拉框直接微调；**改了下拉框，上面的日期会同步跟着变**。
- 保存后，**这一天的课表、桌面小组件和上课提醒会一起换成被补那天的安排**——不需要分别设置。

**改了哪里**

- 提示条文案从「今天按第 4 周周二课表上课」改成更直白的「**今天补第 4 周周二的课**」。
- 新增「**取消**」按钮。选错了点一下就能恢复按实际周次上课。
- 取消后**调休日的记录仍然保留**，提示条不会消失，你随时可以重新设置。

**保留的功能**：右上角菜单的「检查国家调休」不变，仍会联网读取国家公布的调休上班日。

> 顺带修掉两个会崩溃的隐患：选完日期后下拉框不刷新；以及先设了较晚的周次、之后又把学期改短时，设置弹窗会直接报错。现在都会自动处理。

---

## 四、桌面小组件

**修复的问题**

- **倒计时出现负数。** 已修。
- **倒计时不自动刷新，必须点开 App 才更新。** 这是本轮比较隐蔽的一个问题：此前为了消除负数，把走时的计时控件换成了静态文字，但刷新闹钟只在几个固定时间点排程，于是数字不再自己走。现在改为**每次都把下一次刷新重新排程**（省电档临近上课每分钟、更远时每 5 分钟），并在没有后续安排时于次日零点兜底重算，不再停在旧值。

**新增能力**

- **刷新频率三档**，可按续航偏好选择：
  - **省电（默认）**：临近上课（2 小时内）每分钟刷新，更远时每 5 分钟，最省电。
  - **正常**：始终每分钟刷新。
  - **精确**：倒计时精确到秒。秒数由桌面进程的计时控件自行走动，**不依赖唤醒 App**，因此不会因系统限制而停住；耗电明显更高。
- **小组件独立配色**：可以单独给小组件设颜色，文字颜色会自动调整为对比色保证可读；带圆角的背景样式会保留。此设置**只影响普通状态**，临近上课时的提醒色不变。

---

## 五、上课提醒

- **修复有强通知时本应用提醒不显眼。**
- **修复已显示的提醒被应用自己清除。**
- **备忘录提醒文案**改为「距离事件开始」，与课程提醒的「距离上课」区分开。
- **新增备忘录专用提前时间**，可以和课程的提前时间分别设置。

---

## 六、界面与外观

**新增**

- **整周课表页**：把整个学期的课铺在一张网格上，每张卡片都标着周次（例如 `1-16周`、`1-16周(单)`）。支持双指缩放和拖动，重叠的课程会并排显示。点标题栏的方框图标可一键看全整张表。**点任意卡片都能打开课程详情。**
- **左右滑动切换日期**，并带滑入动画。想一次滑一周，可在菜单里把「滑动切换」改成「一周」。
- **主题色自选**：12 个预设色，另支持用滑杆自定义取色。
- **主界面背景图**：选图后可以拖动和缩放决定显示哪一块，框内即实际效果；另有「遮罩浓度」和「课表透明度」两个滑杆，避免背景图干扰课程文字。

**修复**

- 整周课表缩放时回弹、默认缩放过大。
- 整周课表节次时间被遮挡。
- **整周课表里右下方的卡片点不动。** 此前只有靠左上角的课程能点开详情，越往右下越没反应。根因是缩放与内容溢出的嵌套顺序不对，导致落在视口外的内容收不到点击。现已改为与 Flutter 官方一致的结构，任何位置的卡片都能点开。
- 有背景图时顶部未铺满、文字看不清。
- 窄屏手机上日期栏文字溢出、错位。

---

## 七、新增功能

- **备忘录**：用来记录开会、交作业这类非课程安排。时间可选「按周重复」（周几 + 节次）或「一次性」（具体日期 + 时刻）。备忘录会出现在课表里、参与提醒，也会显示在桌面小组件上。
- **课上完后自动显示第二天课表**：当天课程全部结束后重新进入 App，会自动切到第二天，不必手动翻。

---

## 八、其它改进

- 应用名统一为 **TJ Class Schedule**。
- **使用教程扩充到 12 步**，覆盖上面所有新功能；调休那一步已按新的设置方式改写，另新增「点课程看详细信息」一步。
- 自动化测试从 63 项增加到 **225 项**，代码静态检查无告警。

---

## 安装与升级提示

- 本次仍为 **debug 签名**。覆盖安装旧版本时，部分机型会提示签名冲突，**需要先卸载旧版**再安装。
- 安装或修改课表后，**请打开 App 一次**让桌面小组件同步。
- 如果小组件是新加的、或者刚升级新版看不到变化，**把小组件从桌面删掉再重新添加**。
- 想让提醒在锁屏后准时到达，请到「提醒设置」打开国产 Android 后台设置教程，允许自启动与后台活动，并关闭电池优化。

---

## 已知限制

- **调休需要你手动指定补哪一天。** 国家通知不包含学校的具体补课安排；学校校历以图片形式发布，无法自动读取，因此这一步保留手动设置，但已简化为「选一个日期」。
- **节假日与调休数据内置到 2026 年。** 之后年份可通过「检查国家调休」联网更新。
- 数据全部保存在本机，**没有账号、没有云同步**。

> 本应用非同济大学官方应用，不代学校提供服务，请以学校教务通知和实际教学安排为准。
